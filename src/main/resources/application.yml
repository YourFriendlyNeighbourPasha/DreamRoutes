spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/dreamroutes-db
    username: postgres
    password: postgres
  flyway:
    enabled: false

server:
  port: 8082
